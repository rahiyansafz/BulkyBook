@{
    ViewData["Title"] = "Categories";
}

@model IEnumerable<Category>

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-dark">List of Categories</h2>
        </div>
        <div class="col-6 text-end">
            <a class="btn btn-dark" asp-controller="Category" asp-action="Create">
                <i class="bi bi-plus-circle"></i> &nbsp;Create New Category
            </a>
        </div>
        <hr />
    </div>

    <br />

    <div class="py-3 px-4">
        <table class="table caption-top table-bordered">
            @*<caption>List of Categories</caption>*@
            <thead>
                <tr>
                    <th scope="col" class="text-center">Category Name</th>
                    <th scope="col" class="text-center">Display Order</th>
                    <th scope="col" class="text-center">Action</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var obj in Model)
                {
                    <tr>
                        @*                <th scope="row">1</th>
                        *@
                        <td class="text-center">@obj.Name</td>
                        <td class="text-center">@obj.DisplayOrder</td>
                        <td class="text-center">
                            <div class="d-grid gap-2 d-md-block">
                                <a class="btn btn-dark" role="button" asp-controller="Category" asp-action="Edit" asp-route-id="@obj.Id"> <i class="bi bi-pencil-square"></i></a>
                                <a class="btn btn-dark" role="button" asp-controller="Category" asp-action="Remove" asp-route-id="@obj.Id"><i class="bi bi-trash3"></i></a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>